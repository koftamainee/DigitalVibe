# PostgreSQL Service Dockerfile (optional)
FROM postgres:latest

# Optional: Add any custom initialization scripts if necessary
# COPY ./init-scripts /docker-entrypoint-initdb.d/

# Expose the default PostgreSQL port
EXPOSE 5432
